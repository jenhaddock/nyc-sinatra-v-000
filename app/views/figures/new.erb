<form action="/figures" method="post">

  Figure Name: <input id="figure_name" type="text" name="figure[name]">

  <br></br>

  Select Titles:
  <ul>
    <% Title.all.each do |t| %>
      <li><input id="title_<%= t.id %>" type="checkbox" name="figure[title_ids][]" value="<%= t.id %>"><%=t.name%></input></li>
    <% end %>
  </ul>

  <br></br>

  <label>and/or, create a new title:</label>
  <label>name:</label>
    <input id="new_title" type="text" name="title[name]"></input>

  <br></br>

  Select Landmarks:
  <ul>
    <% Landmark.all.each do |l| %>
      <li><input id="landmark_<%= l.id %>" type="checkbox" name="figure[landmark_ids][]" value="<%= l.id %>"><%=l.name%></input></li>
    <% end %>
  </ul>

  <br></br>

  <label>and/or, create a new landmark:</label>
  <br></br>
  <label>name:</label>
    <input id="new_landmark" type="text" name="landmark[name]"></input>

  <br></br>

  <button type="submit" value="Create New Figure">Create New Figure</button>
</form>
